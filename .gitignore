# Local-only dev JWT keypair (generated per-machine, never real prod keys)
keys/
dev.sqlite3

# Secrets — was committed by mistake (contained real Neon credentials);
# rotated and untracked. Populate locally/on the server, never commit.
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
venv/
